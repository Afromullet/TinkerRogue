Consider replacing row based tareting with just cells. I.E, attacking row one is just 3 cells

Simplify Unit data. No need for weapons, armor, etc in the json and for units

Make sure units attack units in front of them. I,E. if a unit attacks a column, it should only attack the column that its facing. 
Same thing with row attacks

There is both a config package and config.go in game_main package. Fix that

Look at the other todos

Review all of the solutions that use caching. See whether we can just create something once rather than caching it. 

Identify where there is a possiblity of cache invalidation errors. We started to cache things used for ECS query, so we need to 
make sure that we update the cache wherever required. List out all of the things that are cached, and where those values are used.
Then develop ap lan from there

Buy Unit issue. Sometimes, the buy unit menu transitions to a "owned unit" window. Seems like it messes up the state transition
Things we're in the a different view. Notice the issue as follows 1) Open Buy Units, buy some. 2) Open edit squads window 3) Go back to buy units window


Remove the map in Guiqueries. 

Check which combat queries should use views

Add proper squad/unit cleanup on death