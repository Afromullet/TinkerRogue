Consider replacing row based tareting with just cells. I.E, attacking row one is just 3 cells

Simplify Unit data. No need for weapons, armor, etc in the json and for units

Make sure units attack units in front of them. I,E. if a unit attacks a column, it should only attack the column that its facing. 
Same thing with row attacks

There is both a config package and config.go in game_main package. Fix that

Look at the other todos

Review all of the solutions that use caching. See whether we can just create something once rather than caching it. 